object thirty {
def main(args:Array[String]):Unit = {

  var age = 22
  var FreeTrial = true

  if (age < 25 || FreeTrial == true){
    println("You are eligible for student discount & FreeTrial")
  }else {
    println("you are not eligible for student discount & FreeTrial")
  }
}
}
